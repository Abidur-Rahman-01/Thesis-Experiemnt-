services:
  postgres:
    image: postgres:16
    profiles: ["storage"]
    environment:
      POSTGRES_USER: veritas
      POSTGRES_PASSWORD: veritas
      POSTGRES_DB: veritas
    ports:
      - "5432:5432"
    volumes:
      - veritas-postgres:/var/lib/postgresql/data

volumes:
  veritas-postgres:
